---
name: sync-plan
description: Commit and push plan file changes with the standard plan sync commit message
---

Use this when asked to sync, save, finalize, delete, commit, or push plan files.

Only stage changes under `plans/`.
Verify the staged diff is plan-only.
Commit with `sync plans` and push.
