---
name: sync-plan
description: Commit and push plan file changes with the standard plan sync commit message
---

Use this when asked to sync, finalize, delete, commit, or push plan files.

Only stage changes under `plans/`.
Read the staged diff to confirm it contains only plan file changes.
Commit with the exact message `sync plans`.
Push.
